name: facetrip
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core:
  firebase_auth:
  firebase_storage:
  cloud_firestore:

  # Dartz libraries
  dartz:

  # Flutter Bloc libraries
  flutter_bloc: ^7.3.3
  bloc:

  # GetIt libraries
  get_it:
  injectable:

  path_provider:
  flutter_dotenv:
  equatable:
  reactive_forms:
  flutter_signin_button:

  #annotations
  json_annotation:
  freezed_annotation:

  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator:

  # Freezed libraries
  build_runner:
  freezed:

  json_serializable:
  flutter_lints:

flutter:
  uses-material-design: true

  assets:
    - .env


